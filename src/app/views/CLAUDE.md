# views/

> L2 | 父级: ../../../CLAUDE.md

## 成员

- `AcceptInviteView.tsx`：企业成员邀请接受与账号初始化。
- `AccountSettingsView.tsx`：当前用户账号资料与安全设置。
- `CargoReviewWorkspace.tsx`：解析校验动画、货物审阅与装柜规则确认工作台。
- `HistoryView.tsx`：历史装箱方案列表与任务入口。
- `LoginView.tsx`：用户登录与角色初始化。
- `RulesConfigView.tsx`：企业装箱规则、柜型与载重限制配置。
- `StartPackingView.tsx`：串联清单上传、解析审阅、装箱计算与结果切换。
- `SystemAdminView.tsx`：企业成员、角色与系统管理。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
